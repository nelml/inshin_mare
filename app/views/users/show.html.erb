<h1>マイページ</h1>
<div class="row">
	<div class="col-xs-4">
		<%= render "shared/user_info" %>
	</div>

	<div clsss="col-xs-4">
		<div class="row">
			<h2>これから何をしましょうか？</h2>
			<h3><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>　新しく<%= link_to "記事を書く", new_article_path %></h3>
			<h3><span class="glyphicon glyphicon-download-alt" aria-hidden="true"></span>　新しい<%= link_to "ジャンルを登録する", new_genre_path %></h3>
			<h3><span class="glyphicon glyphicon-comment" aria-hidden="true"></span>　<%= link_to "他の人に記事", articles_path %>にコメントを書く</h3><br>
			<h2>自分の記事一覧</h2>
				<% if @articles.present? %>
					<% @articles.each do |article| %>
						<h4 class="br20">　　　<span class="glyphicon glyphicon-book" aria-hidden="true"></span>　<%= link_to article.title, article_path(article.id) %></h4><br>
					<% end %>
					<%= paginate @articles %>
				<% else %>
					投稿記事がありません。<%= link_to "投稿", new_article_path %>してみましょう。
				<% end %>
		</div>
	</div>
</div>
